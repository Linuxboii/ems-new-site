---
import Layout from '@/layouts/Layout.astro';
import PageHero from '@/components/PageHero.astro';
import GallerySection from '@/components/GallerySection.astro';
import { gallery } from '@/data/gallery';
import { galleryJsonLd, breadcrumbJsonLd } from '@/lib/galleryJsonLd';

const title = 'Gallery - ESD Packing & Coating Photos | EMS Technologies';
const description = 'Photo gallery of EMS Technologies ESD packing: anti-static and static shielding bags, EVA bins, EPE foam inserts, PET and HIPS thermoformed trays, material swatches and handling.';
const path = '/gallery/';

// One section per product line, in the same order as the products menu.
const sections = [
  { key: 'bubble', id: 'bubble-bags', blurb: 'Anti-static bubble bags cushioning assembled boards in transit.' },
  { key: 'shield', id: 'shielding-bags', blurb: 'Metallised static shielding bags forming a Faraday cage around the device.' },
  { key: 'grid', id: 'grid-bags', blurb: 'Conductive grid bags with the diamond pattern, for visible-content shielding.' },
  { key: 'pink', id: 'pink-bags', blurb: 'LDPE pink anti-static bags for general electronics packing.' },
  { key: 'bins', id: 'eva-bins', blurb: 'EVA ESD bins holding circuit boards, labelled for electrostatic-sensitive devices.' },
  { key: 'evafoam', id: 'epe-foam', blurb: 'EPE foam inserts routed to component geometry for cushioned, static-safe handling.' },
  { key: 'traysPet', id: 'pet-trays', blurb: 'Clear PET thermoformed trays for component storage and transport.' },
  { key: 'traysHips', id: 'hips-trays', blurb: 'Black HIPS conductive thermoformed trays, with and without clear lids.' },
  { key: 'swatch', id: 'materials', blurb: 'Material and colour swatch samples used across the ESD packing range.' },
  { key: 'handling', id: 'handling', blurb: 'ESD-safe handling of components on the shop floor.' },
].map((s) => ({ ...s, group: gallery[s.key] }));

const jsonLd = [
  breadcrumbJsonLd([
    { name: 'Home', path: '/' },
    { name: 'Gallery', path },
  ]),
  await galleryJsonLd('EMS Technologies Gallery', description, path, sections.map((s) => s.group)),
];
---
<Layout title={title} description={description} path={path} jsonLd={jsonLd}>
  <PageHero eyebrow="Photos" title="Gallery" lead="Product photography across the EMS Technologies ESD packing range: bags, bins, foam inserts, thermoformed trays and the materials they are made from.">
    <nav class="mt-8 flex flex-wrap gap-2" aria-label="Gallery sections">
      {sections.map((s) => (
        <a href={`#${s.id}`} class="rounded-lg border border-line bg-surface px-3 py-1.5 text-sm text-muted transition hover:border-accent2 hover:text-accent2">{s.group.label}</a>
      ))}
    </nav>
  </PageHero>
  {sections.map((s) => (
    <GallerySection id={s.id} title={s.group.label} enquire={s.group.label} images={s.group.images} blurb={s.blurb} />
  ))}
  <section class="section border-t border-line">
    <div class="container-x text-center max-w-2xl">
      <h2 class="heading text-2xl sm:text-3xl">Looking for something in a custom size?</h2>
      <p class="mt-3 text-muted">Tell us the part and the quantity, and we will quote the right packing for it.</p>
      <a href="/contact-us/" class="btn-primary mt-7">Get a quote</a>
    </div>
  </section>
</Layout>
